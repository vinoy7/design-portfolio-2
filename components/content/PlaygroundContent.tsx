"use client";

export default function PlaygroundContent() {
  return (
    <div
      className="flex items-center justify-center"
      style={{
        height: "400px",
        background: "#f0f0f0",
        fontFamily: "var(--font-dm-sans)",
        fontSize: "19px",
        color: "#757575",
      }}
    >
      Playground content — coming soon (Figma rate limit hit during planning)
    </div>
  );
}
